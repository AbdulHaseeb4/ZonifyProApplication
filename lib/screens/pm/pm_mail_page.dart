import 'package:flutter/material.dart';

class PMMailPage extends StatelessWidget {
  const PMMailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PM Mail Page"));
  }
}
